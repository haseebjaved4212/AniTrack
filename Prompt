I'm building AniTrack, a full-stack anime tracking application. I need you to build the BACKEND ONLY, step by step, in the phases below. Complete and explain each phase before moving to the next — do not skip ahead or combine phases.

TECH STACK:
- Framework: FastAPI (Python)
- Database: PostgreSQL
- Cache/Queue: Redis
- External API: Jikan API (https://jikan.moe/) as the anime metadata source
- ORM: SQLAlchemy (async) with Alembic for migrations
- Auth: JWT-based authentication

PROJECT GOAL:
Users can search anime (pulled from Jikan API), add anime to a personal list, track watch status (Watching / Completed / Plan to Watch / Dropped / On Hold), rate anime, and see basic stats about their list.

---

PHASE 1 — Project Setup
- Initialize the FastAPI project with a clean folder structure (app/, app/api/, app/models/, app/schemas/, app/core/, app/db/, app/services/)
- Set up requirements.txt / pyproject.toml with all needed dependencies
- Set up config management using pydantic-settings, reading from a .env file (DB URL, Redis URL, JWT secret, Jikan API base URL)
- Set up the async PostgreSQL connection with SQLAlchemy
- Set up Alembic for migrations
- Give me the exact commands to run to get this running locally

PHASE 2 — Database Models
- Design and create SQLAlchemy models for: User, Anime (cached from Jikan), UserAnimeEntry (the join table: user's list entry with status, rating, progress, notes)
- Generate the first Alembic migration
- Explain each model's fields and relationships before writing code

PHASE 3 — Authentication
- Implement user registration and login endpoints
- Password hashing with passlib/bcrypt
- JWT access token generation and a dependency to protect routes
- Add a /me endpoint to fetch the current logged-in user

PHASE 4 — Jikan API Integration
- Build a service layer that wraps calls to the Jikan API (search anime, get anime by ID)
- Implement caching in Redis so repeated searches don't hit Jikan's rate limits
- Add endpoints: GET /anime/search?q=, GET /anime/{id}
- Handle Jikan's rate limiting gracefully (they allow ~3 requests/second)

PHASE 5 — User Anime List (Core Feature)
- Endpoints to: add anime to user's list, update status/rating/progress, remove from list, get full list (with filtering by status)
- Make sure adding an anime caches its Jikan data locally in the Anime table so we're not re-fetching every time

PHASE 6 — Stats & Extras
- Endpoint that returns basic stats for a user: total anime watched, total episodes watched, average rating given, breakdown by status
- Add pagination to list/search endpoints

PHASE 7 — Polish
- Add proper error handling and consistent error response format
- Add CORS config for the Next.js frontend (localhost:3000 for dev)
- Add a health check endpoint
- Write a README with setup instructions and a list of all endpoints with example requests/responses

For each phase: show me the code, briefly explain key decisions, and tell me what to test before I move to the next phase. Ask me clarifying questions if anything about the requirements is ambiguous rather than assuming.


I'm building AniTrack, a full-stack anime tracking application. The backend is already built with FastAPI, PostgreSQL, and Redis, using JWT auth and the Jikan API as the anime metadata source. Now I need you to build the FRONTEND ONLY, step by step, in the phases below. Complete and explain each phase before moving to the next, do not skip ahead or combine phases.

TECH STACK:
- Framework: Next.js (App Router) with TypeScript
- Styling: TailwindCSS
- State/data fetching: React Query (TanStack Query) for server state
- Forms: React Hook Form + Zod for validation
- HTTP client: a typed fetch wrapper (no axios needed)

BACKEND API CONTRACT (already built, connect to this):
- POST /auth/register, POST /auth/login (returns JWT)
- GET /auth/me
- GET /anime/search?q=
- GET /anime/{id}
- GET /user/anime-list (supports status filter + pagination)
- POST /user/anime-list (add anime with status/rating/progress)
- PATCH /user/anime-list/{id} (update status/rating/progress)
- DELETE /user/anime-list/{id}
- GET /user/stats

---

PHASE 1 — Project Setup
- Initialize Next.js with TypeScript, TailwindCSS, App Router
- Set up folder structure: app/, components/, lib/, types/, hooks/
- Set up .env.local / .env.example with NEXT_PUBLIC_API_URL
- Set up TypeScript types matching the backend response shapes (User, Anime, UserAnimeEntry, Stats)
- Set up a typed API client in lib/api.ts that handles base URL, JSON headers, and attaches the JWT token to protected requests
- Give me the exact commands to get this running locally

PHASE 2 — Auth Flow
- Build login and register pages
- Store JWT securely (httpOnly cookie preferred over localStorage, explain the tradeoff either way)
- Build an auth context/hook (useAuth) to track logged-in state across the app
- Build a protected route wrapper so certain pages redirect to /login if not authenticated
- Add a logout function

PHASE 3 — Anime Search & Discovery
- Build a search page that hits GET /anime/search with debounced input
- Display results in a responsive grid (poster, title, score, status badge)
- Build an anime detail page (GET /anime/{id}) showing synopsis, genres, episodes, score, and an "Add to My List" button
- Handle loading and empty states properly

PHASE 4 — My List (Core Feature)
- Build the main dashboard/list page showing the user's tracked anime
- Filter tabs by status: Watching / Completed / Plan to Watch / Dropped / On Hold
- Each entry card should allow inline status change, rating (star or number input), and progress update (episodes watched)
- Add a remove-from-list action with a confirmation step
- Use React Query mutations with optimistic updates so the UI feels instant

PHASE 5 — Stats Page
- Build a stats page hitting GET /user/stats
- Show total anime watched, total episodes, average rating, and a simple chart/breakdown by status (use a lightweight chart lib like Recharts)

PHASE 6 — Polish & UX
- Add a responsive navbar (logo, search, my list, stats, profile/logout)
- Add loading skeletons for search results and list views
- Add toast notifications for actions (added to list, status updated, errors)
- Add proper error boundaries and a 404 page
- Make sure everything is mobile responsive

PHASE 7 — Final Review
- Add a README with setup instructions
- Do a pass checking all API calls handle errors gracefully (expired token, network failure, 404s)
- Confirm CORS works correctly against the local backend

For each phase: show me the code, briefly explain key decisions, and tell me what to test before I move to the next phase. Ask me clarifying questions if anything about the requirements is ambiguous rather than assuming.